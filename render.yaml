services:
  - type: web
    name: talcadatos
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python3 server.py"
    envVars:
      - key: ADMIN_PASSWORD
        generateValue: true
